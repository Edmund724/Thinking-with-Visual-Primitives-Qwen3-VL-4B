# Stage 4a: Specialized GRPO — Box Expert
model_path: outputs/stage3a_sft_box
output_dir: outputs/stage4a_grpo_box
max_completion_length: 384
per_device_batch_size: 2
gradient_accumulation_steps: 2
learning_rate: 1.0e-6
num_train_epochs: 1
lora_r: 256
lora_alpha: 512
lora_dropout: 0.05
optimizer: paged_adamw_8bit
bf16: true
warmup_steps: 50
logging_steps: 10
save_steps: 200
save_total_limit: 2
gradient_checkpointing: true
report_to: none
num_samples: 2000
num_counting: 1000
num_clevr: 1000
num_rounds: 2
num_generations: 2
beta: 0.04
temperature: 1.0
filter_batch_size: 4
filter_max_completion_length: 384
filter_empty_cache_every: 50
skip_difficulty_filter: false
early_stopping_subset_size: 0
early_stopping_eval_steps: 50
early_stopping_patience: 2
iou_thresholds: [0.3, 0.5]
use_quality_rm_api: false
compile_model: false
early_stopping_subset_size: 16
early_stopping_eval_steps: 50
early_stopping_patience: 2
